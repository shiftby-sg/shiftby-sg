# Phase 5 - Performance Optimization

Purpose: Optimize Core Web Vitals.

Expected outcome: Low LCP and fast rendering.

Mode: `bounded execution`

Must validate:

- LCP
- CLS
- INP where measurable
- static rendering
- image/font optimization
- bundle hygiene
- mobile performance

Evidence expectation for future runs:

- `workpacket_root`
- `phase_id`
- `run_id`
- `tested_condition`
- `expected_outcome`
- `actual_outcome`
- `passes`
- `blockers`
- `created_at` or `generated_at` where available
